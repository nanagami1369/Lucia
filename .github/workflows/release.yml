name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Build CLI Installer
        run: |
          dotnet publish src/Lucia.Installer/Lucia.Installer.csproj `
            --configuration Release

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            src/Lucia.Installer/bin/Release/net10.0-windows/win-x64/publish/Lucia.Installer.exe
          append_body: true
          body: |
            インストール方法は [ユーザーガイド](https://github.com/nanagami1369/Lucia/blob/main/docs/USER_GUIDE.md) を参照してください。
